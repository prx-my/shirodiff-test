# shirodiff-test

Minimal Next.js app used to test the [shiroDiff](https://github.com/apps/shirodiff) GitHub App.

Install shiroDiff on this repo, open a PR with a visual change, and the bot posts a
before/after/diff comment with pixel + SSIM scores.

```bash
npm install
npm run dev
```
